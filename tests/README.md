# tests

Active test suite for Chat DSL.

This directory now contains the active tests for:

- `apps/streamlit/`
- `chatdsl_core/`

Historical version-specific tests should remain with their archived snapshots.
