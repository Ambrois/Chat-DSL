# apps/streamlit

Active Streamlit app for Chat DSL.

This directory now contains:

- the active app entrypoint
- app-specific helpers such as `dsl_render_utils.py`
- active state under `apps/streamlit/state/`
