# archive

Historical version snapshots for Chat DSL.

This directory now contains:

- `v0.1/`
- `v0.2/`
- `v0.3/`

Phase 2 status:

- `v0.1/`, `v0.2/`, and `v0.3/` moved here under issue `#11`
- `v0.4/` remains at the repo root for compatibility wrappers and persisted state until final cleanup
